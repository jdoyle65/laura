import styled from "styled-components";

export default (el) => styled(el)`
  display: inline;
  background-image: linear-gradient(
    to top,
    rgba(199, 124, 242, 0.7) 40%,
    transparent 40%
  );
`;
